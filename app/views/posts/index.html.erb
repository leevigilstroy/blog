<% provide(:title, "Posts") %>

<div>
  <% @posts.each do |post| %>
  <div>
    <h2><%=post.title%></h2>
    <div>
      <%=post.content%>
      <p><%=post.created_at%></p>
    </div>
  </div>  
  
  <% end %>
</div>